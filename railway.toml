[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install -r stock_analyzer/requirements.txt && uvicorn stock_analyzer.web_server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/login"
restartPolicyType = "on_failure"
